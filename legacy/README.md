# TindaKart legacy desktop application

This directory contains the original Java Swing desktop application preserved during the platform migration.

- `app/` contains the legacy Swing source code.
- `tindakart/` contains the original launcher folder.
- `run.bat` is the original desktop launcher.

The current backend is located at the repository root under `backend/`.
