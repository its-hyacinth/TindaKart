# TindaKart legacy desktop application

This directory contains the original Java Swing desktop application preserved during the PWA migration.

- `app/` contains the legacy Swing source code.
- `tindakart/` contains the original launcher folder.
- `run.bat` is the original desktop launcher.

The new PWA and backend are located at the repository root under `frontend/` and `backend/`.
