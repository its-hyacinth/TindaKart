import { useState, type ReactNode } from 'react'
import { Activity, BarChart3, Boxes, CreditCard, LayoutDashboard, Menu, Package, Settings, ShieldCheck, Store, X } from 'lucide-react'
import { Button } from '../ui/button'
import { cn } from '../../lib/utils'

export type AdminView = 'overview' | 'vendors' | 'plans' | 'billing' | 'audit' | 'settings'

const navigation: { id: AdminView; label: string; icon: typeof LayoutDashboard }[] = [
  { id: 'overview', label: 'Overview', icon: LayoutDashboard },
  { id: 'vendors', label: 'Vendors & stores', icon: Store },
  { id: 'plans', label: 'Plans & pricing', icon: Package },
  { id: 'billing', label: 'Billing activity', icon: CreditCard },
  { id: 'audit', label: 'Audit log', icon: Activity },
]

export function AdminShell({ view, onViewChange, onSignOut, username, children }: { view: AdminView; onViewChange: (view: AdminView) => void; onSignOut: () => void; username: string; children: ReactNode }) {
  const [open, setOpen] = useState(false)
  function navigate(next: AdminView) { onViewChange(next); setOpen(false) }
  return <div className="min-h-screen bg-slate-100 text-slate-950"><aside className={cn('fixed inset-y-0 left-0 z-40 flex w-64 flex-col border-r border-slate-200 bg-slate-950 text-white transition-transform lg:translate-x-0', open ? 'translate-x-0' : '-translate-x-full')}><div className="flex h-16 items-center justify-between border-b border-white/10 px-4"><div className="flex items-center gap-2.5"><span className="grid size-8 place-items-center rounded-lg bg-indigo-400 text-xs font-black text-slate-950">TK</span><div><strong className="block text-sm">TindaKart</strong><span className="block text-[9px] font-bold uppercase tracking-[.15em] text-slate-400">Platform console</span></div></div><button className="lg:hidden" onClick={() => setOpen(false)} aria-label="Close menu"><X size={18} /></button></div><div className="flex-1 px-3 py-5"><p className="px-2 text-[9px] font-bold uppercase tracking-[.18em] text-slate-500">Super Admin</p><nav className="mt-2 space-y-0.5">{navigation.map(item => { const Icon = item.icon; return <button key={item.id} onClick={() => navigate(item.id)} className={cn('flex w-full items-center gap-2.5 rounded-lg px-2.5 py-2.5 text-left text-[13px] font-semibold transition', view === item.id ? 'bg-indigo-500 text-white shadow-lg shadow-indigo-950/20' : 'text-slate-400 hover:bg-white/10 hover:text-white')}><Icon size={16} />{item.label}</button> })}</nav><div className="mt-6 border-t border-white/10 pt-4"><button onClick={() => navigate('settings')} className={cn('flex w-full items-center gap-2.5 rounded-lg px-2.5 py-2.5 text-left text-[13px] font-semibold text-slate-400 hover:bg-white/10 hover:text-white', view === 'settings' && 'bg-white/10 text-white')}><Settings size={16} />Platform settings</button></div></div><div className="border-t border-white/10 p-3"><div className="mb-2 flex items-center gap-2.5 rounded-lg bg-white/5 p-2.5"><span className="grid size-8 place-items-center rounded-full bg-indigo-400 text-[11px] font-black text-slate-950">{username.slice(0, 1).toUpperCase()}</span><div className="min-w-0"><strong className="block truncate text-[13px]">{username}</strong><span className="text-[11px] text-slate-400">Super Admin</span></div></div><Button variant="ghost" className="w-full justify-start px-2.5 text-[13px] text-slate-400 hover:bg-white/10 hover:text-white" onClick={onSignOut}>Sign out</Button></div></aside>{open && <button className="fixed inset-0 z-30 bg-slate-950/50 lg:hidden" onClick={() => setOpen(false)} aria-label="Close menu overlay" />}<div className="lg:pl-64"><header className="sticky top-0 z-20 flex h-16 items-center justify-between border-b border-slate-200 bg-white/90 px-4 backdrop-blur lg:px-6"><button className="rounded-lg p-1.5 hover:bg-slate-100 lg:hidden" onClick={() => setOpen(true)} aria-label="Open menu"><Menu size={20} /></button><div className="hidden items-center gap-2.5 text-[13px] text-slate-500 sm:flex"><ShieldCheck size={16} className="text-indigo-600" /> Platform administration</div><div className="ml-auto flex items-center gap-2.5"><span className="hidden rounded-full bg-emerald-50 px-2.5 py-1 text-[11px] font-bold text-emerald-700 sm:inline-flex">All systems operational</span><span className="grid size-8 place-items-center rounded-full bg-slate-900 text-[11px] font-black text-white">{username.slice(0, 1).toUpperCase()}</span></div></header><main className="mx-auto max-w-[1500px] p-4 lg:p-6">{children}</main></div></div>
}

export function AdminPageHeader({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: ReactNode }) {
  return <div className="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-[11px] font-bold uppercase tracking-[.17em] text-indigo-600">{eyebrow}</p><h1 className="mt-1.5 text-3xl font-black tracking-[-.055em] text-slate-950">{title}</h1><p className="mt-2 max-w-2xl text-[13px] leading-5 text-slate-500">{description}</p></div>{action && <div className="shrink-0">{action}</div>}</div>
}

export function StatCard({ label, value, detail, icon: Icon, tone = 'indigo' }: { label: string; value: string | number; detail: string; icon: typeof BarChart3; tone?: 'indigo' | 'emerald' | 'amber' | 'rose' }) {
  const tones = { indigo: 'bg-indigo-50 text-indigo-600', emerald: 'bg-emerald-50 text-emerald-600', amber: 'bg-amber-50 text-amber-600', rose: 'bg-rose-50 text-rose-600' }
  return <div className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-[13px] font-semibold text-slate-500">{label}</p><strong className="mt-1.5 block text-2xl font-black tracking-tight text-slate-950">{value}</strong></div><span className={cn('grid size-9 place-items-center rounded-lg', tones[tone])}><Icon size={17} /></span></div><p className="mt-3 text-[11px] font-semibold text-slate-500">{detail}</p></div>
}
