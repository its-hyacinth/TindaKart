import { useEffect, useState, type ReactNode } from 'react'
import { ArrowRight, BarChart3, Check, Download, Package, ScanLine, ShieldCheck, Smartphone, Sparkles } from 'lucide-react'
import { publicApi, type PublicPackage } from '../api'
import { Badge } from '../components/ui/badge'
import { Button } from '../components/ui/button'
import { Card, CardContent } from '../components/ui/card'

const defaultLimits: Record<string, Record<string, number>> = { Free: { MAX_STORES: 1, MAX_PRODUCTS: 100, MAX_STAFF: 0 }, Growth: { MAX_STORES: 3, MAX_PRODUCTS: 2500, MAX_STAFF: 5 }, Pro: { MAX_STORES: 100000, MAX_PRODUCTS: 100000, MAX_STAFF: 100000 } }
const limitLabel = (value: number | undefined) => value === undefined ? '—' : value >= 100000 ? 'Unlimited' : value.toLocaleString()

export function LandingPage({ onLogin, onRegister, onInstall, installed, installMessage }: { onLogin: () => void; onRegister: () => void; onInstall: () => void; installed: boolean; installMessage: string }) {
  const [plans, setPlans] = useState<PublicPackage[]>([])
  useEffect(() => { publicApi.packages().then(setPlans).catch(() => setPlans([])) }, [])
  return <main className="min-h-screen overflow-hidden bg-slate-50 text-slate-950"><div className="absolute inset-x-0 top-0 -z-0 h-[38rem] bg-[radial-gradient(circle_at_75%_15%,#dfe3ff,transparent_34rem)]" /><nav className="relative z-10 mx-auto flex w-full max-w-7xl items-center justify-between px-5 py-5 lg:px-8"><a href="#top" className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-slate-950 text-xs font-black text-white">TK</span><span><strong className="block text-sm">TindaKart</strong><small className="block text-[10px] font-bold uppercase tracking-[.14em] text-slate-500">Store operations</small></span></a><div className="hidden items-center gap-8 text-sm font-semibold text-slate-500 md:flex"><a href="#features">Features</a><a href="#plans">Plans</a><a href="#mobile">Mobile ready</a></div><div className="flex items-center gap-2"><Button variant="ghost" size="sm" onClick={onLogin}>Sign in</Button><Button variant="secondary" size="sm" onClick={onRegister}>Start free</Button></div></nav><section id="top" className="relative z-10 mx-auto grid w-full max-w-7xl gap-14 px-5 pb-20 pt-16 lg:grid-cols-[.95fr_1.05fr] lg:items-center lg:px-8 lg:pb-28 lg:pt-20"><div><Badge><Sparkles size={12} /> Built for local stores</Badge><h1 className="mt-6 max-w-2xl text-6xl font-black leading-[.92] tracking-[-.08em] sm:text-7xl lg:text-[6.4rem]">Sell with less chaos.</h1><p className="mt-7 max-w-xl text-lg leading-8 text-slate-600">A modern POS and store workspace for your tindahan - simple enough to start free, powerful enough to grow with your team.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Button size="lg" onClick={onRegister}>Create free account <ArrowRight size={18} /></Button><Button variant="secondary" size="lg" onClick={onInstall}><Download size={17} /> {installed ? 'App installed' : 'Install the app'}</Button></div>{installMessage && <p className="mt-3 text-sm font-semibold text-emerald-700" role="status">{installMessage}</p>}<div className="mt-8 flex flex-wrap gap-x-5 gap-y-2 text-xs font-semibold text-slate-500"><span className="inline-flex items-center gap-1.5"><Check size={14} className="text-emerald-600" /> Free owner plan</span><span className="inline-flex items-center gap-1.5"><Check size={14} className="text-emerald-600" /> No card required</span><span className="inline-flex items-center gap-1.5"><Check size={14} className="text-emerald-600" /> Mobile ready</span></div></div><DashboardPreview /></section><section id="features" className="relative z-10 mx-auto grid w-full max-w-7xl gap-4 px-5 pb-20 sm:grid-cols-2 lg:grid-cols-4 lg:px-8"><Feature icon={<ScanLine />} title="Sell faster" text="Search, scan, cart, payment, and receipt in one focused flow." /><Feature icon={<Package />} title="Know your stock" text="See receiving, low stock, and expiration signals clearly." /><Feature icon={<ShieldCheck />} title="Stay in control" text="Vendor-scoped access keeps every store and staff action accountable." /><Feature icon={<Smartphone />} title="Take it anywhere" text="Use the PWA on laptop, tablet, or phone at the counter." /></section><section id="plans" className="mx-auto w-full max-w-7xl px-5 pb-24 lg:px-8"><div className="mb-8 max-w-2xl"><Badge>Plans that grow with you</Badge><h2 className="mt-4 text-4xl font-black tracking-[-.06em] sm:text-5xl">Start simple. Add power when you need it.</h2><p className="mt-4 text-base leading-7 text-slate-600">Choose a package or start with the Free owner plan. Need something more flexible? With the Custom plan, you only pay for the staff seats and features you want to unlock.</p></div><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{plans.length ? plans.map(plan => <PlanCard key={plan.id} plan={plan} onRegister={onRegister} />) : <p className="text-sm text-slate-500">Plans are loading...</p>}</div><Card className="mt-5 border-indigo-100 bg-indigo-50"><CardContent className="flex flex-col gap-3 p-5 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-bold text-indigo-950">Custom plan</p><p className="mt-1 text-xs leading-5 text-indigo-800">Buy additional staff seats and unlock only the features your store needs. Super Admin pricing is applied transparently.</p></div><Button variant="secondary" onClick={onRegister}>Talk to us <ArrowRight size={15} /></Button></CardContent></Card></section><footer id="mobile" className="border-t border-slate-200 bg-white px-5 py-8 text-center text-xs font-semibold text-slate-500">TindaKart · Secure · Responsive · Store-ready</footer></main>
}

function DashboardPreview() { return <div className="relative"><div className="absolute -inset-5 rounded-[3rem] bg-indigo-200/40 blur-3xl" /><Card className="relative overflow-hidden rounded-[2rem] border-white/80 shadow-2xl shadow-indigo-950/15"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><span className="text-xs font-bold text-slate-500">Today at your store</span><span className="rounded-lg bg-indigo-50 px-2 py-1 text-[10px] font-bold text-indigo-700">LIVE</span></div><div className="grid grid-cols-[58px_1fr]"><aside className="flex min-h-[360px] flex-col items-center gap-6 bg-slate-950 py-5"><span className="grid size-8 place-items-center rounded-lg bg-indigo-400 text-[10px] font-black">TK</span><BarChart3 size={18} className="text-indigo-300" /><Package size={18} className="text-indigo-300" /><ScanLine size={18} className="text-indigo-300" /></aside><div className="bg-white p-6 sm:p-8"><p className="text-xs font-bold uppercase tracking-[.15em] text-indigo-600">Store overview</p><h2 className="mt-2 text-2xl font-black">Everything in one view.</h2><div className="mt-7 grid grid-cols-2 gap-3"><div className="rounded-2xl border border-slate-100 bg-slate-50 p-4"><span className="text-xs font-semibold text-slate-500">Today's sales</span><strong className="mt-2 block text-2xl font-black">PHP 24,680</strong></div><div className="rounded-2xl border border-amber-100 bg-amber-50 p-4"><span className="text-xs font-semibold text-amber-800">Stock alerts</span><strong className="mt-2 block text-2xl font-black text-amber-950">08</strong></div></div><div className="mt-5 flex h-24 items-end gap-2 rounded-2xl border border-slate-100 p-4">{[42, 60, 48, 72, 64, 84, 96].map((height, index) => <span key={index} className="flex-1 rounded-t-md bg-indigo-200 first:bg-indigo-600" style={{ height: `${height}%` }} />)}</div></div></div></Card></div> }
function PlanCard({ plan, onRegister }: { plan: PublicPackage; onRegister: () => void }) { const isFree = plan.monthlyPrice === 0; const limits = plan.limits ?? {}; return <Card className={`h-full ${isFree ? 'border-indigo-200 ring-1 ring-indigo-100' : ''}`}><CardContent className="flex h-full flex-col p-6"><div className="flex items-start justify-between"><div><h3 className="text-lg font-bold">{plan.name}</h3><p className="mt-1 text-xs text-slate-500">{plan.description}</p></div>{isFree && <span className="rounded-full bg-indigo-50 px-2 py-1 text-[10px] font-bold text-indigo-700">START HERE</span>}</div><p className="mt-6 text-3xl font-black">{isFree ? 'Free' : `PHP ${plan.monthlyPrice.toLocaleString()}`}<small className="text-xs font-semibold text-slate-400">{!isFree && ' / month'}</small></p><p className="mt-1 text-xs text-slate-400">{isFree ? 'No card required' : `PHP ${plan.annualPrice.toLocaleString()} billed annually`}</p><ul className="mt-6 space-y-2 text-xs text-slate-600">{(plan.features ?? []).slice(0, 6).map(feature => <li key={feature} className="flex gap-2"><Check size={14} className="text-emerald-600" />{feature.replaceAll('_', ' ')}</li>)}</ul><div className="mt-auto grid grid-cols-3 gap-2 border-t border-slate-100 pt-5 text-center"><div><strong className="block text-sm">{limitLabel(limits.MAX_STORES)}</strong><span className="text-[10px] text-slate-400">Stores</span></div><div><strong className="block text-sm">{limitLabel(limits.MAX_PRODUCTS)}</strong><span className="text-[10px] text-slate-400">Products</span></div><div><strong className="block text-sm">{limitLabel(limits.MAX_STAFF)}</strong><span className="text-[10px] text-slate-400">Staff</span></div></div><Button variant={isFree ? 'accent' : 'secondary'} className="mt-5 w-full" onClick={onRegister}>{isFree ? 'Create free account' : 'Get started'} <ArrowRight size={15} /></Button></CardContent></Card> }
function Feature({ icon, title, text }: { icon: ReactNode; title: string; text: string }) { return <Card className="rounded-2xl shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><CardContent className="p-5"><div className="mb-5 grid size-10 place-items-center rounded-xl bg-indigo-50 text-indigo-600">{icon}</div><h3 className="font-bold text-slate-950">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p></CardContent></Card> }
