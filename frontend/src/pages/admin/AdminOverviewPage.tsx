import { Activity, ArrowRight, BarChart3, CreditCard, Package, Store, Users } from 'lucide-react'
import { Card, CardContent, CardHeader } from '../../components/ui/card'
import { AdminPageHeader, StatCard } from '../../components/admin/AdminShell'
import type { CurrentUser, Vendor } from '../../api'

export function AdminOverviewPage({ user, vendors, onViewChange }: { user: CurrentUser; vendors: Vendor[]; onViewChange: (view: 'vendors' | 'plans' | 'billing' | 'audit') => void }) {
  const active = vendors.filter(vendor => vendor.status === 'ACTIVE').length
  const pending = vendors.filter(vendor => vendor.status === 'PENDING').length
  return <><AdminPageHeader eyebrow="Platform overview" title={`Good morning, ${user.username}.`} description="Monitor the TindaKart platform, support vendors, and keep plans and access under control." /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Total vendors" value={vendors.length} detail={`${active} active accounts`} icon={Store} /><StatCard label="Pending review" value={pending} detail="Accounts needing attention" icon={Activity} tone="amber" /><StatCard label="Active stores" value={user.stores.length} detail="Visible in your current session" icon={BarChart3} tone="emerald" /><StatCard label="Platform role" value="Owner" detail="Full platform administration" icon={Users} tone="rose" /></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.3fr_.7fr]"><Card><CardHeader><div className="flex items-center justify-between"><div><h2 className="font-bold text-slate-950">Vendor activity</h2><p className="mt-1 text-sm text-slate-500">Your platform accounts at a glance.</p></div><button onClick={() => onViewChange('vendors')} className="inline-flex items-center gap-1 text-sm font-bold text-indigo-600">View vendors <ArrowRight size={15} /></button></div></CardHeader><CardContent><div className="space-y-3">{vendors.slice(0, 5).map(vendor => <div key={vendor.id} className="flex items-center justify-between rounded-xl border border-slate-100 p-4"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-slate-100 text-sm font-black text-slate-700">{vendor.name.slice(0, 1).toUpperCase()}</span><div><strong className="block text-sm">{vendor.name}</strong><span className="text-xs text-slate-500">Vendor #{vendor.id}</span></div></div><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${vendor.status === 'ACTIVE' ? 'bg-emerald-50 text-emerald-700' : vendor.status === 'SUSPENDED' ? 'bg-rose-50 text-rose-700' : 'bg-amber-50 text-amber-700'}`}>{vendor.status}</span></div>)}{vendors.length === 0 && <div className="rounded-xl border border-dashed border-slate-200 p-10 text-center text-sm text-slate-500">No vendors have been created yet.</div>}</div></CardContent></Card><div className="space-y-6"><Card><CardHeader><h2 className="font-bold text-slate-950">Admin shortcuts</h2></CardHeader><CardContent className="grid gap-2"><button onClick={() => onViewChange('vendors')} className="flex items-center gap-3 rounded-xl bg-slate-50 p-4 text-left hover:bg-indigo-50"><Store size={18} className="text-indigo-600" /><span><strong className="block text-sm">Manage vendors</strong><small className="text-xs text-slate-500">Review accounts and stores</small></span></button><button onClick={() => onViewChange('plans')} className="flex items-center gap-3 rounded-xl bg-slate-50 p-4 text-left hover:bg-indigo-50"><Package size={18} className="text-indigo-600" /><span><strong className="block text-sm">Configure pricing</strong><small className="text-xs text-slate-500">Plans and custom add-ons</small></span></button><button onClick={() => onViewChange('billing')} className="flex items-center gap-3 rounded-xl bg-slate-50 p-4 text-left hover:bg-indigo-50"><CreditCard size={18} className="text-indigo-600" /><span><strong className="block text-sm">Billing activity</strong><small className="text-xs text-slate-500">Payment operations</small></span></button></CardContent></Card><Card className="border-indigo-100 bg-indigo-50"><CardContent className="p-5"><p className="text-xs font-bold uppercase tracking-[.16em] text-indigo-600">Session scope</p><p className="mt-3 text-sm leading-6 text-indigo-950">Signed in as <strong>{user.username}</strong> with {user.roles.join(', ')} access.</p></CardContent></Card></div></div></>
}
